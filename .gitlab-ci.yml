variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - precheck
  - benchmark

default:
  

build-thesis:
  stage: precheck
  only:
    - main
    - staging
    - thesis
  image: "texlive/texlive"
  script:
    - "cd docs/thesis"
    - "latexmk --pdf thesis.tex"
    - "cd ../proposal"
    - "latexmk --pdf proposal.tex"
  artifacts:
    paths:
      - "docs/thesis/thesis.pdf"
      - "docs/proposal/proposal.pdf"
    expire_in: 1 month
  tags:
    - docker

cpu-scoring-benchmark:
  stage: benchmark
  only:
    - cpu_scoring_benchmark
  tags:
    - noctua2
  variables:
    SCHEDULER_PARAMETERS: "-A hpc-lco-kenter -p normal -t 00:10:00 -c 8 --mem 8G"
    DPCPP_CPU_NUM_CUS: 8
  before_script:
    - "ml fpga devel intel/oneapi bittware/520n Boost CMake"
    - "python3 -m venv .venv"
    - "source .venv/bin/activate"
    - "pip install --upgrade pip"
    - "pip install -r requirements.txt"
    - "mkdir -p build"
    - "cd build"
    - "../tool.py generate -o . -n 63 -m 64 -a 1e-6 -b 0.42 -e 0.4"
    - "cmake -DCMAKE_BUILD_TYPE=Release .."
    - "make cpu_scoring_benchmark"
  script:
    - "./cpu_scoring_benchmark/cpu_scoring_benchmark -i input.csv -r 42 -l 1000000"
